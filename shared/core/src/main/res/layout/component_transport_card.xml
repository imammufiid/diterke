<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="80dp"
    android:layout_height="80dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="6dp"
    android:id="@+id/component_transport_card_root_view"
    android:background="@drawable/bg_component_transport_card_unselected">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/component_transport_card_image"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginTop="4dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/component_transport_card_title"
        app:srcCompat="@drawable/ic_transport_bike"/>

    <TextView
        android:id="@+id/component_transport_card_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="6dp"
        android:textSize="8sp"
        tools:text="TransBike"
        android:textColor="@color/black"
        tools:ignore="SmallSp" />


</androidx.constraintlayout.widget.ConstraintLayout>